<!DOCTYPE html>
<html lang="en">
<head>
    <% include ./partials/_header %>
</head>
<body>

    <h1><% urls %></h1>
    
<ul>
    <% for (var shortUrl in urls) { %>
    <li>
           
  <form method="POST" action=<%="/urls/" + shortUrl+ "/delete" %>>
<%= shortUrl %> <%= urls[shortUrl] %><button type="submit">DELETE</button>

</form>

</li>
<% } %>  
</ul>
  
    <a href="#">Shorten String</a>
</body>
</html>